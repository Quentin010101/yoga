import { theme } from '../theme'

export const Footer = ()=>{

    const palette = theme()

    return (
        <footer style={{backgroundColor: palette.primary.normal}}>
            <div>
                
            </div>
        </footer>
    )
}